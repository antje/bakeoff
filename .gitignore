# Secrets
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
dist/

# Bench runs, at bench/ or in any subdirectory (--out bench/<name>). Results
# carry provider names, dates, and costs from a specific key and are regenerated
# per run; the reports worth keeping live in examples/<name>/results-*.md.
bench/**/results-*
bench/**/*.log
bench/**/*.png

# Node (linter only)
node_modules/

# Editors / OS
.DS_Store
.vscode/
.idea/

# Claude Code local state
.claude/

# Local demo warm-up script
scripts/warmup.sh